name: Deploy to Staging

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3  # Use the latest version of the checkout action

      - name: Deploy Application
        run: |
          cd C:/xampp/htdocs/test/github # Project path

          git config --global user.email "jezraelsalino@gmail.com"
          git config --global user.name "JezraelSalino"

          # Pull the latest updates from the repository
          git pull origin staging

          gh pr create --base main --head staging --title "New Pull Request Title" --body "There are new updaates from staging branch"

          # Add additional deployment steps here (e.g., build, restart services)
